# Netlify configuration. Security headers live in public/_headers (copied to dist/).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# The app uses hash routing (#/merge etc.), so no SPA rewrite is needed.
